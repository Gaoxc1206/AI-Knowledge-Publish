---
type: "concept"
status: "enriched"
category: "其他"
domain: "待分类"
background: "included"
---
# 凸优化

## 标准定义

{"category":"理论概念","domain":"[[大语言模型]][[多目标对齐]]","standard_definition":"凸优化是指一类优化问题，其中[[黑盒 oracle|目标函数]]是凸函数，且可行域由凸约束构成。其核心性质是：局部最优解也是全局最优解，因而这类问题通常具有较好的理论分析与求解稳定性。常见形式包括线性规划、二次规划、凸二次规划、半正定规划等。","in_vault_usage":"在这篇论文中，凸优化不是直接用来描述整个[[大语言模型|语言模型]]参数训练过程，而是被用作多目标对齐中的一个可解近似子问题。[[PAMA]] 将原本依赖高维梯度的多目标 min-norm 组合问题，重写为关于目标权重系数的凸优化问题，并进一步利用 [[Noon PPO]] 的结构得到[[闭式解]]。这个凸优化子问题只与目标数量相关，从而把原先接近 O(n^2 d) 的计算开销降到 O(n)，服务于多目标奖励之间的 Pareto 折中。","key_points":["标准上，凸优化强调“目标凸 + 约束凸”，因此具有全局最优与可分析性；但在这篇工作里，它主要出现为 [[多目标优化]] 的一个低成本替代求解器。","论文并没有把整个 [[Large Language Model|LLM]] 对齐过程变成凸问题，而是把多目标 [[PPO]] 中的[[梯度聚合]]/权重选择部分改写为凸优化。","PAMA 依托 [[Noon PPO]] 的非负优势截断，把原始高维梯度 min-norm 问题近似为对 \(c^{(i)}\) 的凸组合优化。","该凸优化子问题给出闭式解，等价于把 0 投影到若干优势值区间上，从而避免显式构造高维梯度 Gram matrix。","在本知识库中，‘凸优化’更多是指一种用于多目标对齐的可计算重写，而不是独立研究的通用优化算法。"],"external_background":["凸集与凸函数是凸优化的基础：若任意两点连线仍在集合内，则集合是凸的；若函数满足 Jensen 型不等式，则

## 在本知识库中的用法

待从更多论文中补充。

## 关键点

- 待从更多论文中补充。

## 别名

- 无

## 外部背景

- 待核对经典来源。

## 相关论文

- [[2025 - 语言模型的帕累托多目标对齐 - heParetoMultiObjectiveAlignment2025]]
